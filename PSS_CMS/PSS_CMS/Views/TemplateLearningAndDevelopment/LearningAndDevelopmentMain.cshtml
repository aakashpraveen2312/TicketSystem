
@{ ViewBag.Title = "LearningAndDevelopmentMain";
    Layout = null; }

@using GridMvc.Html

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="~/Content/Style1.css" rel="stylesheet" />
    <link href="~/Content/MainMenuTicket.css" rel="stylesheet" />
    <link href="@Url.Content("~/Content/Gridmvc.css")" rel="stylesheet" />
    <link href="@Url.Content("~/Content/bootstrap.min.css")" rel="stylesheet" />
    <script src="@Url.Content("~/Scripts/jquery-1.9.1.min.js")"></script>
    <script src="@Url.Content("~/Scripts/gridmvc.min.js")"></script>
    @*Bootstrap Icons and Boxicons link*@
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href='https://unpkg.com/boxicons@2.1.1/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="..." crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body onload="hideLoadingScreen()">
    <div class="sidebar">
        <a class="text-decoration-none" href="@Url.Action("RecentTicket","RecentTickets")">Recent Tickets</a>
        <a href="@Url.Action("Clients","RecentTickets")" class="text-decoration-none">Client</a>
        <a href="javascript:void(0);" class="dropdown-btn text-decoration-none">CMS <i class="bi bi-chevron-down dropdown-btn"></i></a>
        <div class="dropdown-container">
            <a href='@Url.Action("Index","RecentTickets")' class="text-decoration-none">Menu</a>
            <a href='@Url.Action("HomeMainContent","RecentTickets")' class="text-decoration-none">Home</a>
            <div class="text-decoration-none servicemenu">
                <a href='@Url.Action("SubmenuForServices","RecentTickets")' class="text-decoration-none">
                    Services<i class="bi bi-chevron-down dropdown-btns"></i>
                </a>
                <div class="submenu" id="submenu-services">
                    <a href='@Url.Action("SubmenuForHRMS","RecentTickets")' class="text-decoration-none">HRMS....</a>

                </div>
            </div>
            <a href='@Url.Action("Clients","RecentTickets")' class="text-decoration-none">Clients</a>
            <a href='@Url.Action("ClientImageGet","ClientImage", new { section = "Client Section"})' class="text-decoration-none">Client Section</a>
            <a href="#clients" class="text-decoration-none">Gallery</a>
            <a href="#clients" class="text-decoration-none">FAQ</a>
            <a href="#clients" class="text-decoration-none">About</a>
            <a href='@Url.Action("Clients","RecentTickets")' class="text-decoration-none">Contact Us</a>


        </div>
        <a href="@Url.Action("Logout", "Login")" class="text-decoration-none align-baseline" id="logoutlink" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasBottom" aria-controls="offcanvasBottom">
            <i class='bx bx-log-out' style="font-size:24px !important;color:red !important;"></i>
            Logout
        </a>
    </div>
    <nav class="nav">
        <h3 class="" id="navheading">TICKET SYSTEM</h3>
    </nav>

    <div class="content">

        <div class="row" style="margin-top:10px">
            <div class="col-lg-9 col-sm-8">
                <ul class="breadcrumb mt-3">
                    <li><a href="@Url.Action("Index", "Home")">Site</a></li>
                    <li>Learning and Development</li>
                    <li>@Session["ContentName"]</li>
                </ul>
            </div>
        </div>
        <!--MAIN CONTAINER BEGINS HERE-->
        <div class="container-fluid ms-0">

            @{ // Get the session values

                var herosectionlearninganddevelopment = Session["PS_T_LearningAndDevelopmentMAIN_TYPE"] as string;
                var learninganddevelopmentaboutus = Session["PS_LT_ABOUT_TYPE"] as string;
                var learninganddevelopmentwhyus = Session["PS_T_LearningAndDevelopmentWHYUS_TYPE"] as string;
                var learninganddevelopmentskill = Session["PS_T_LearningAndDevelopmentSKILL_TYPE"] as string; }

            <!-- Main Section ends here -->
            @if (!string.IsNullOrEmpty(herosectionlearninganddevelopment) && herosectionlearninganddevelopment == "Header+SubHeader+Image")
            {
        <div class="row g-2 d-flex justify-content-center">
            <h3 class=" d-flex justify-content-center">Hero Section In Home Page</h3>
            <div class="card shadow-lg overflow-hidden">
                @using (Html.BeginForm("LearningAndDevelopmentMainImageSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form2", enctype = "multipart/form-data" }))
                {
        <div class="col-12 col-md-12 col-lg-12 d-flex justify-content-center">

            <div class="card-body">
                @if (ViewBag.LEARNINGANDDEVELOPMENTMAINIMAGE != null && !string.IsNullOrEmpty(ViewBag.LEARNINGANDDEVELOPMENTMAINIMAGE.ToString()))
                {
                    string mimeType = ViewBag.MAINMIMEType?.ToString() ?? "image/jpeg";  // Default to JPEG if MIMEType is not set       <!-- Display the image if it exists -->
        <div class="d-flex justify-content-center align-items-center">

            <img id="imagePreview" src="data:@mimeType;base64,@ViewBag.LEARNINGANDDEVELOPMENTMAINIMAGE" class="img-fluid animated" alt="" style="max-width:200px;" />
            <input type="hidden" id="existingImageMain" name="ExistingImageMain" value="@ViewBag.LEARNINGANDDEVELOPMENTMAINIMAGE" />
        </div> }
    else
    { <!-- Display a placeholder if no image exists -->
                                <div class="d-flex justify-content-center align-items-center">
                                    <img id="imagePreview" src="https://via.placeholder.com/200" class="img-fluid animated" alt="" />
                                </div>}

                <div class="d-flex justify-content-between align-items-center">
                    <!-- File input for image upload -->
                    <input type="file" class="form-control" name="file" id="fileInput" accept="image/*" />
                </div>
            </div>

        </div>}


                @using (Html.BeginForm("LearningAndDevelopmentMainTextSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form" }))
                {
        <div class="col-12 col-md-12 col-lg-6">
            <div class="card-body">
                @if (ViewBag.LearningAndDevelopmentMainText != null && ViewBag.LearningAndDevelopmentMainText.Count > 0)
                {
        <div class="form-floating">
            <!-- Display the first value (index 0) in the first card -->
            <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea1"
                      name="psValues[0]">@ViewBag.LearningAndDevelopmentMainText[0]</textarea>
        </div>}
            </div>

        </div>

                            <div class="col-12 col-md-12 col-lg-6">

                                <div class="card-body">
                                    @if (ViewBag.LearningAndDevelopmentMainText != null && ViewBag.LearningAndDevelopmentMainText.Count > 1)
                                    {
                <div class="form-floating">
                    <!-- Display the second value (index 1) in the second card -->
                    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                              name="psValues[1]">@ViewBag.LearningAndDevelopmentMainText[1]</textarea>
                </div>}
                                </div>
                            </div>

                                                <div class="col-12 col-sm-12 col-md-12 col-lg-12 d-flex justify-content-end mb-3">
                                                    <div class="d-flex justify-content-end gap-2">
                                                        <button class="btn btn-success login" type="submit">Save</button>
                                                        <button class="btn btn-danger login" type="button" onclick="window.location.href='@Url.Action("Index", "Home")'">Cancel</button>
                                                    </div>
                                                </div>}

            </div>
        </div> }

    else if (!string.IsNullOrEmpty(learninganddevelopmentaboutus) && learninganddevelopmentaboutus == "TEXT")
    {
        <div class="row g-2 d-flex justify-content-center mt-5  mb-5">
            <h3 class=" d-flex justify-content-center">Learning And Development Page About US</h3>
            <div class="card shadow-lg p-4">
                @using (Html.BeginForm("LearninganddevelopmentAboutUsSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form3" }))
                {<!-- Card 1 and Card 2 in the first row -->
                                        <div class="col-12 col-md-12 col-lg-12">
                                            <div class="card-body d-flex justify-content-center">
                                                @if (ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS != null && ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS.Count > 0)
                                                {
                            <div class="form-floating">

                                <input class="form-control about-us" name="psValues[0]" value="@ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS[0]">

                            </div>}
                                            </div>

                                        </div>
                                                            <div class="col-12 col-md-12 col-lg-12 d-felx justify-content-center">
                                                                <div class="card-body">

                                                                    @if (ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS != null && ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS.Count > 0)
                                                                    {
                                                <div class="form-floating mt-3">
                                                    <textarea class="form-control" name="psValues[1]">@ViewBag.LEARNINGANDDEVELOPMENTCONTENTMAINABOUTUS[1]</textarea>
                                                </div>}
                                                                </div>

                                                            </div>
                                                                                <div class="col-12 col-md-12 col-lg-12 d-flex justify-content-end ">
                                                                                    <div class="d-flex justify-content-end gap-2">
                                                                                        <button class="btn btn-success login" type="submit">Save</button>
                                                                                        <button class="btn btn-danger login" type="button" onclick="window.location.href='@Url.Action("Index", "Home")'">Cancel</button>
                                                                                    </div>
                                                                                </div>}


            </div>


        </div> }

    else if (!string.IsNullOrEmpty(learninganddevelopmentwhyus) && learninganddevelopmentwhyus == "Header+Paragraph+Image")
    {
        <div class="row g-2 d-flex justify-content-center">
            <h3 class=" d-flex justify-content-center">Hero Section In Home Page</h3>
            <div class="card shadow-lg overflow-hidden">
                @using (Html.BeginForm("LearningAndDevelopmentWhyUsImageSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form2", enctype = "multipart/form-data" }))
                {
        <div class="col-12 col-md-12 col-lg-12 d-flex justify-content-center">

            <div class="card-body">
                @if (ViewBag.LEARNINGANDDEVELOPMENTWHYUSIMAGE != null && !string.IsNullOrEmpty(ViewBag.LEARNINGANDDEVELOPMENTWHYUSIMAGE.ToString()))
                {
                    string mimeType1 = ViewBag.WHYUSMIMEType?.ToString() ?? "image/jpeg";  // Default to JPEG if MIMEType is not set       <!-- Display the image if it exists -->
        <div class="d-flex justify-content-center align-items-center">

            <img id="imagePreview" src="data:@mimeType1;base64,@ViewBag.LEARNINGANDDEVELOPMENTWHYUSIMAGE" class="img-fluid animated" alt="" style="max-width:200px;" />
            <input type="hidden" id="existingImageMain" name="ExistingImageMain" value="@ViewBag.LEARNINGANDDEVELOPMENTWHYUSIMAGE" />
        </div> }
    else
    { <!-- Display a placeholder if no image exists -->
                                <div class="d-flex justify-content-center align-items-center">
                                    <img id="imagePreview" src="https://via.placeholder.com/200" class="img-fluid animated" alt="" />
                                </div>}

                <div class="d-flex justify-content-between align-items-center">
                    <!-- File input for image upload -->
                    <input type="file" class="form-control" name="file" id="fileInput" accept="image/*" />
                </div>
            </div>

        </div>}

                @using (Html.BeginForm("LearningAndDevelopmentWhyUsTextSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form" }))
                {
        <div class="col-12 col-md-12 col-lg-6">
            <div class="card-body">
                @if (ViewBag.LearningAndDevelopmentWhyUSText != null && ViewBag.LearningAndDevelopmentWhyUSText.Count > 0)
                {
        <div class="form-floating">
            <!-- Display the first value (index 0) in the first card -->
            <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea1"
                      name="psValues[0]">@ViewBag.LearningAndDevelopmentWhyUSText[0]</textarea>
        </div>}
            </div>

        </div>

                            <div class="col-12 col-md-12 col-lg-6">

                                <div class="card-body">
                                    @if (ViewBag.LearningAndDevelopmentWhyUSText != null && ViewBag.LearningAndDevelopmentWhyUSText.Count > 1)
                                    {
                <div class="form-floating">
                    <!-- Display the second value (index 1) in the second card -->
                    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                              name="psValues[1]">@ViewBag.LearningAndDevelopmentWhyUSText[1]</textarea>
                </div>}
                                </div>
                            </div>

                                                <div class="col-12 col-md-12 col-lg-6">

                                                    <div class="card-body">
                                                        @if (ViewBag.LearningAndDevelopmentWhyUSText != null && ViewBag.LearningAndDevelopmentWhyUSText.Count > 1)
                                                        {
                                    <div class="form-floating">
                                        <!-- Display the second value (index 1) in the second card -->
                                        <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                                  name="psValues[2]">@ViewBag.LearningAndDevelopmentWhyUSText[2]</textarea>
                                    </div>}
                                                    </div>
                                                </div>

                                                                    <div class="col-12 col-sm-12 col-md-12 col-lg-12 d-flex justify-content-end mb-3">
                                                                        <div class="d-flex justify-content-end gap-2">
                                                                            <button class="btn btn-success login" type="submit">Save</button>
                                                                            <button class="btn btn-danger login" type="button" onclick="window.location.href='@Url.Action("Index", "Home")'">Cancel</button>
                                                                        </div>
                                                                    </div>}

            </div>
        </div> }

    else if (!string.IsNullOrEmpty(learninganddevelopmentskill) && learninganddevelopmentskill == "Header+Paragraph+Image")
    {
        <div class="row g-2 d-flex justify-content-center">
            <h3 class=" d-flex justify-content-center">Hero Section In Home Page</h3>
            <div class="card shadow-lg overflow-hidden">
                @using (Html.BeginForm("LearningAndDevelopmentSkillImageSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form2", enctype = "multipart/form-data" }))
                {
            <div class="col-12 col-md-12 col-lg-12 d-flex justify-content-center">

                <div class="card-body">
                    @if (ViewBag.LEARNINGANDDEVELOPMENTSKILLIMAGE != null && !string.IsNullOrEmpty(ViewBag.LEARNINGANDDEVELOPMENTSKILLIMAGE.ToString()))
                    {
                        string mimeTypeLearningskill = ViewBag.SKILLSMIMEType?.ToString() ?? "image/jpeg";  // Default to JPEG if MIMEType is not set       <!-- Display the image if it exists -->
            <div class="d-flex justify-content-center align-items-center">

                <img id="imagePreview" src="data:@mimeTypeLearningskill;base64,@ViewBag.LEARNINGANDDEVELOPMENTSKILLIMAGE" class="img-fluid animated" alt="" style="max-width:200px;" />
                <input type="hidden" id="existingImageMain" name="ExistingImageMain" value="@ViewBag.LEARNINGANDDEVELOPMENTSKILLIMAGE" />
            </div> }
        else
        { <!-- Display a placeholder if no image exists -->
                                    <div class="d-flex justify-content-center align-items-center">
                                        <img id="imagePreview" src="https://via.placeholder.com/200" class="img-fluid animated" alt="" />
                                    </div>}

                    <div class="d-flex justify-content-between align-items-center">
                        <!-- File input for image upload -->
                        <input type="file" class="form-control" name="file" id="fileInput" accept="image/*" />
                    </div>
                </div>

            </div>}

                @using (Html.BeginForm("LearningAndDevelopmentSkillTextSave", "TemplateLearningAndDevelopment", FormMethod.Post, new { id = "form" }))
                {
            <div class="col-12 col-md-12 col-lg-6">
                <div class="card-body">
                    @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 0)
                    {
            <div class="form-floating">
                <!-- Display the first value (index 0) in the first card -->
                <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea1"
                          name="psValues[0]">@ViewBag.LearningAndDevelopmentSkillText[0]</textarea>
            </div>}
                </div>

            </div>

                                <div class="col-12 col-md-12 col-lg-6">

                                    <div class="card-body">
                                        @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 1)
                                        {
                    <div class="form-floating">
                        <!-- Display the second value (index 1) in the second card -->
                        <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                  name="psValues[1]">@ViewBag.LearningAndDevelopmentSkillText[1]</textarea>
                    </div>}
                                    </div>
                                </div>

                                                    <div class="col-12 col-md-12 col-lg-6">

                                                        <div class="card-body">
                                                            @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 1)
                                                            {
                                        <div class="form-floating">
                                            <!-- Display the second value (index 1) in the second card -->
                                            <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                                      name="psValues[2]">@ViewBag.LearningAndDevelopmentSkillText[2]</textarea>
                                        </div>}
                                                        </div>
                                                    </div>
                                                                        <div class="col-12 col-md-12 col-lg-6">

                                                                            <div class="card-body">
                                                                                @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 1)
                                                                                {
                                                            <div class="form-floating">
                                                                <!-- Display the second value (index 1) in the second card -->
                                                                <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                                                          name="psValues[3]">@ViewBag.LearningAndDevelopmentSkillText[3]</textarea>
                                                            </div>}
                                                                            </div>
                                                                        </div>
                                                                                            <div class="col-12 col-md-12 col-lg-6">

                                                                                                <div class="card-body">
                                                                                                    @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 1)
                                                                                                    {
                                                                                <div class="form-floating">
                                                                                    <!-- Display the second value (index 1) in the second card -->
                                                                                    <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                                                                              name="psValues[4]">@ViewBag.LearningAndDevelopmentSkillText[4]</textarea>
                                                                                </div>}
                                                                                                </div>
                                                                                            </div>
                                                                                                                <div class="col-12 col-md-12 col-lg-6">

                                                                                                                    <div class="card-body">
                                                                                                                        @if (ViewBag.LearningAndDevelopmentSkillText != null && ViewBag.LearningAndDevelopmentSkillText.Count > 1)
                                                                                                                        {
                                                                                                    <div class="form-floating">
                                                                                                        <!-- Display the second value (index 1) in the second card -->
                                                                                                        <textarea class="form-control" placeholder="Leave a comment here" id="floatingTextarea2"
                                                                                                                  name="psValues[5]">@ViewBag.LearningAndDevelopmentSkillText[5]</textarea>
                                                                                                    </div>}
                                                                                                                    </div>
                                                                                                                </div>

                                                                                                                                    <div class="col-12 col-sm-12 col-md-12 col-lg-12 d-flex justify-content-end mb-3">
                                                                                                                                        <div class="d-flex justify-content-end gap-2">
                                                                                                                                            <button class="btn btn-success login" type="submit">Save</button>
                                                                                                                                            <button class="btn btn-danger login" type="button" onclick="window.location.href='@Url.Action("Index", "Home")'">Cancel</button>
                                                                                                                                        </div>
                                                                                                                                    </div>}






            </div>
        </div> }

    else
    { @*<script>
    window.location.href = '@Url.Action("HomeMainContent","RecentTickets")'
    </script>*@
                        <h4>NO DATA AVAILABLE</h4>}

            <!-- Main Section ends here -->

        </div> <!--MAIN CONTAINER ENDS HERE-->




        <div class="offcanvas offcanvas-bottom" tabindex="-1" id="offcanvasBottom" aria-labelledby="offcanvasBottomLabel" style="height: 150px;">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title text-align-center" id="offcanvasBottomLabel"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body d-flex flex-column justify-content-center align-items-center">
                <h4>Are you sure you want to log out?</h4>
            </div>
            <div class="offcanvas-footer d-flex justify-content-center">
                <input type="button" class="btn btn-success me-3" id="confirmLogoutBtn" value="Confirm" tabindex="1">
                <input type="button" class="btn btn-danger" data-bs-dismiss="offcanvas" value="Cancel" tabindex="2">
            </div>
        </div>
    </div>
    <div id="preloader"></div>

    <!-- LogOut JS -->
    <script src="~/Scripts/Logoutjs.js"></script>

    <script src="~/Scripts/ImageSubmission.js"></script>

    <script>
    @* Script for active menu *@

    document.addEventListener("DOMContentLoaded", function () {
        const sidebarLinks = document.querySelectorAll(".sidebar a");

        // Highlight the active link based on the current URL
        sidebarLinks.forEach(link => {
            if (link.href === window.location.href) {
                link.classList.add("active");
            }
        });

        // Add click event to update the active class
        sidebarLinks.forEach(link => {
            link.addEventListener("click", function () {
                // Remove active class from all links
                sidebarLinks.forEach(l => l.classList.remove("active"));

                // Add active class to the clicked link
                this.classList.add("active");
            });
        });
    });


    @* Script for dropdown *@

        document.addEventListener("DOMContentLoaded", function () {
            const dropdownBtn = document.querySelector(".dropdown-btn");
        const dropdownContainer = document.querySelector(".dropdown-container");

        dropdownBtn.addEventListener("click", function () {
            this.classList.toggle("active");
        dropdownContainer.style.display = dropdownContainer.style.display === "block" ? "none" : "block";
            });
        });


    </script>
    <script>document.getElementById("fileInput").addEventListener("change", function (event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    // Update the image preview
                    const imagePreview = document.getElementById("imagePreview");
                    imagePreview.src = e.target.result;
                    // Clear the hidden field when a new file is selected
                    document.getElementById("existingImageMain").value = "";
                };
                reader.readAsDataURL(file); // Convert file to base64 string
            }
        });

        document.getElementById("fileInputcore").addEventListener("change", function (event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    // Update the image preview
                    const imagePreview = document.getElementById("imagePreviewcore");
                    imagePreview.src = e.target.result;
                    // Clear the hidden field when a new file is selected
                    document.getElementById("existingImagecore").value = "";
                };
                reader.readAsDataURL(file); // Convert file to base64 string
            }
        });


        document.getElementById("fileInputskill").addEventListener("change", function (event) {
            const file = event.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function (e) {
                    // Update the image preview
                    const imagePreview = document.getElementById("imagePreviewskill");
                    imagePreview.src = e.target.result;
                    // Clear the hidden field when a new file is selected
                    document.getElementById("existingImageskill").value = "";
                };
                reader.readAsDataURL(file); // Convert file to base64 string
            }
        });</script>



</body>
</html>










