@model IEnumerable<PSS_CMS.Models.Prioritywise>
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <!-- Favicons -->
    <link href="~/assets/img/bexlogo_wob.png" rel="icon">
    <link href="~/assets/img/bexlogo_wob.png" rel="apple-touch-icon">
    <link href="~/Content/ClientSide.css" rel="stylesheet" />
    <link href="~/Content/Style1.css" rel="stylesheet" />
    <link href="@Url.Content("~/Content/Gridmvc.css")" rel="stylesheet" />
    <link href="@Url.Content("~/Content/bootstrap.min.css")" rel="stylesheet" />
    <script src="@Url.Content("~/Scripts/jquery-1.9.1.min.js")"></script>
    <script src="@Url.Content("~/Scripts/gridmvc.min.js")"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="~/Content/FooterResponsive.css" rel="stylesheet" />
    @*Bootstrap Icons and Boxicons and Fontawesome link*@
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href='https://unpkg.com/boxicons@2.1.1/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" integrity="..." crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <title>BEYONDEXS - SERVICE MANAGEMENT</title>

    <script src="~/Scripts/Sidebarscript.js"></script>
    <link href="~/Content/Responsive.css" rel="stylesheet" />
    <link href="~/Content/Colors.css" rel="stylesheet" />
    <link href="~/Content/SideMenu.css" rel="stylesheet" />
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- JustGage (only if used) -->
    <script src="https://cdn.jsdelivr.net/npm/raphael@2.1.4/raphael-min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/justgage@1.3.0/justgage.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        .card {
            max-width: 100% !important;
            height: 40vh;
            overflow: visible;
        }

        .dropdown-check-list {
            position: relative;
        }

            .dropdown-check-list .anchor {
                cursor: pointer;
                display: inline-block;
                padding: 8px;
                width: 100%;
                border: 1px solid #ccc;
                background: #fff;
            }

            .dropdown-check-list ul.items {
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background: #fff;
                border: 1px solid #ccc;
                max-height: 250px;
                overflow-y: auto;
                display: none;
                list-style: none;
            }

            .dropdown-check-list.visible ul.items {
                display: block;
            }

            .dropdown-check-list ul.items li {
                padding: 6px 10px;
            }

        .contents {
            padding: 0 16px 0 25px !important;
            min-height: 250px !important;
            margin-left: 65px !important;
            background-color: #e4e9f7 !important;
        }
    </style>
</head>

<body>
    @*Sidebar*@
    <nav class="sidebar close">
        <header>
            <div class="text logo-text" style="display:inline-block">

                <a href="@Url.Action("ContractDashboard", "Contract")">


                    @if (Session["Logo"] != null)
                    {
                        <img src="data:image/png;base64,@Session["Logo"]" alt="PSS Logo" class="logo" />
                    }

                </a>


            </div>

            <i class="bx bx-chevron-right toggle" style="margin-right:28px;margin-top:16px;"></i>

        </header>
        <div class="menu-bar">
            <div class="menu">
                <ul class="menu-links" style="margin-left:20px">
                    <!-- ACCOUNTS ADMIN -->
                    <li class="nav-link openfold" style="margin-top:10px;">
                        <img src="~/assets/img/software-engineer.png" class="settingsIcon" style="width:25px;height:25px;margin-bottom:10px;" />
                        <img src="~/assets/img/software-engineer.png" class="openIcon" style="display:none;width:25px;height:25px;margin-bottom:10px;" />
                        <span class="text nav-text" style="margin-left:15px">ACCOUNTS ADMIN</span>
                    </li>
                    <!-- Customer Accounts -->
                    <li class="nav-link">
                        <a href="@Url.Action("CustomerList", "Contract")">
                            <i class='bx bx-file' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Contracts and Users</span>
                        </a>
                    </li>


                    <li class="nav-link">
                        <a href="@Url.Action("Index", "ServiceInvoice")">
                            <i class='bx bx-receipt' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Service Invoice</span>
                        </a>
                    </li>
                    <li class="nav-link openfold" style="margin-top:10px;">
                        <img src="~/assets/img/ReportImage.png" class="settingsIcon" style="width:25px;height:25px;margin-bottom:10px;" />
                        <img src="~/assets/img/ReportImage.png" class="openIcon" style="display:none;width:25px;height:25px;margin-bottom:10px;" />
                        <span class="text nav-text" style="margin-left:15px">REPORTS</span>
                    </li>

                    <li class="nav-link">
                        <a href="@Url.Action("Report", "FollowUpReport")">
                            <i class='bx bx-phone-call' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Follow-up Report</span>
                        </a>
                    </li>
                    <li class="nav-link">
                        <a href="@Url.Action("ContractexpiryReport", "ContractExpiryReport")">
                            <i class='bx bx-time-five' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Contract Expiry</span>
                        </a>
                    </li>
                    <li class="nav-link">
                        <a href="@Url.Action("ProductFinanceReport", "ContractExpiryReport")">
                            <i class='bx bx-credit-card' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Product Finance</span>
                        </a>
                    </li>
                    <li class="nav-link">
                        <a href="@Url.Action("CustomerFinanceReport", "ContractExpiryReport")">
                            <i class='bx bx-wallet' style="font-size:20px"></i>
                            <span class="text nav-text" style="margin-left:15px">Customer Finance</span>
                        </a>
                    </li>
                    <!-- Settings Section -->
                    <li class="nav-link openfold" style="margin-top:10px;">
                        <img src="~/assets/img/cyber-security.png" class="settingsIcon" style="width: 25px; height: 25px; margin-bottom: 5px;" />
                        <img src="~/assets/img/cyber-security.png" class="openIcon" style="display:none;width:25px;height:25px;margin-bottom:5px;" />
                        <span class="text nav-text" style="margin-left:15px">SECURITY</span>
                    </li>

                    @*Change Password*@
                    <li class="nav-link">
                        <a href="@Url.Action("ChangePassword", "Contract")">
                            <i class='bx bxs-key' style="font-size:24px"></i>
                            <span class="text nav-text" style="margin-left:15px">Change Password</span>
                        </a>
                    </li>

                    <!-- Logout -->
                    <li class="nav-link">
                        <a href="@Url.Action("Index", "Login")" class="logout-trigger login-link text-danger" data-bs-toggle="tooltip" data-bs-placement="top" title="Logout">
                            <i class='bx bx-log-out' style="font-size:24px"></i>
                            <span class="text nav-text" style="margin-left:15px">Logout</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="contents">
        <nav class="nav d-flex">
            <div class="p-2 flex-grow-1"> <h3 style="margin-top:10px" class="heading ms-3 adminheadinres">SERVICE MANAGEMENT</h3></div>
            <div class="p-2" style="margin-right:10px"><h4 class="heading adminlogoutres">@Session["UserName"].ToString().ToUpper() - @Session["Role"]</h4></div>
            <div class="p-2 me-3 logout" style="margin-right:10px" data-toggle="tooltip" data-placement="bottom" title="Log out">
                <a href="@Url.Action("Logout", "Login")" class="text-decoration-none" id="logoutlink" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasBottom" aria-controls="offcanvasBottom">
                    <i class='bx bx-log-out' style="font-size:24px;color:red !important;" id="logoutresiconadmin"></i>

                </a>
            </div>

        </nav>
        <div class="row" style="margin-top:10px">
            <div class="col-lg-12">
                <ul class="breadcrumb mt-1" style="margin-bottom:0px!important">
                    <li>CUSTOMER FINANCE REPORT</li>
                </ul>
            </div>
        </div>


        <div class="container mt-5 card shadow p-4 rounded-3">
            <h3 class="mb-4 text-center">Customer Finance Report</h3>

            <!-- ✅ Form starts here -->
            <form method="post" id="financeForm" action="@Url.Action("CustomerFinanceReport", "ContractExpiryReport")">

                <input type="hidden" id="ProductRecid" name="ProductRecid" />
                <input type="hidden" id="ContractRecid" name="ContractRecid" />
                <input type="hidden" id="CustomerRecid" name="CustomerRecid" />
                <input type="hidden" id="Type" name="Type" />



                <div class="row g-3">
                    <!-- Product Combo -->
                    <div class="col-md-3">
                        <label class="form-label fw-bold">Products</label>

                        <div id="productDropdown" class="dropdown-check-list" tabindex="100">
                            <span class="anchor">-- Select Product --</span>
                            <ul class="items" id="productList">
                                <li>
                                    <label>
                                        <input type="checkbox" id="selectAllProducts">
                                        <strong>Select All</strong>
                                    </label>
                                </li>
                                @if (ViewBag.Product != null)
                                {
                                    foreach (var g in ViewBag.Product)
                                    {
                                        <li>
                                            <input type="checkbox" class="productCheck" value="@g.Value" />
                                            @g.Text
                                        </li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>

                    <!-- Contract Combo -->
                    <div class="col-md-3">
                        <label class="form-label fw-bold">Contract Numbers</label>

                        <div id="itemGroupDropdown" class="dropdown-check-list" tabindex="100">
                            <span class="anchor">-- Select Contract Number --</span>
                            <ul class="items" id="contractList">
                                <li>
                                    <label>
                                        <input type="checkbox" id="selectAllItemGroups">
                                        <strong>Select All</strong>
                                    </label>
                                </li>
                                @if (ViewBag.Contractnumber != null)
                                {
                                    foreach (var g in ViewBag.Contractnumber)
                                    {
                                        <li>
                                            <input type="checkbox" class="itemGroupCheck" value="@g.Value" />
                                            @g.Text
                                        </li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>

                    <!-- To Date -->
                    <div class="col-md-3">
                        <label class="form-label fw-bold">Customers</label>

                        <div id="itemCategoryDropdown" class="dropdown-check-list">
                            <span class="anchor">-- Select Customer --</span>
                            <ul class="items" id="itemCategoryList"></ul>
                        </div>
                    </div>

                    <!-- Priority type Combo Box -->
                    <div class="col-md-3">
                        <label class="form-label fw-bold">Select Payment</label>
                        <div id="paymentDropdown" class="dropdown-check-list">
                            <span class="anchor">-- Select Payment --</span>
                            <ul class="items" id="paymentList">
                                <li>
                                    <label>
                                        <input type="checkbox" id="selectAllPayments">
                                        <strong>Select All</strong>
                                    </label>
                                </li>
                                <li>
                                    <input type="checkbox" class="paymentCheck" value="Paid" />
                                    Paid
                                </li>
                                <li>
                                    <input type="checkbox" class="paymentCheck" value="Unpaid" />
                                    Unpaid
                                </li>
                                <li>
                                    <input type="checkbox" class="paymentCheck" value="Partially Paid" />
                                    Partially Paid
                                </li>
                            </ul>
                        </div>

                    </div>
                </div>

                <!-- Submit Button -->
                <div class="text-center mt-5">
                    <button type="submit"
                            name="ActionType"
                            value="Filter"
                            class="btn btn-warning px-4 me-3">
                        Fillter
                    </button>

                    <button type="submit"
                            name="ActionType"
                            value="PDF"
                            class="btn btn-danger px-4 me-3">
                        PDF
                    </button>

                </div>

            </form>
            <!-- ✅ Form ends here -->
        </div>

        <!-- Report Table -->

        <div class="card report-card mt-5 ms-3  mb-5">
            <div class="card-body table-responsive">

                <table class="table table-bordered table-hover">
                    <thead class="table-primary">
                        <tr>
                            <th>Product Name</th>
                            <th>Customer Name</th>
                            <th>Email</th>
                            <th>Mobile</th>
                            <th>Contract Amount</th>
                            <th>Paid Amount</th>
                            <th>Pending Amount</th>
                        </tr>
                    </thead>

                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.ProductName</td>
                                    <td>@item.customerName</td>
                                    <td>@item.customerEmail</td>
                                    <td>@item.customerMobile</td>
                                    <td class="text-right">@item.ContractAmount</td>  
                                    <td class="text-right">@item.PaidAmount</td>
                                    <td class="text-right">@item.PendingAmount</td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-muted text-center">
                                    No records found
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
                <div class="d-flex justify-content-start mt-3">
                    <button id="prevBtn" class="btn btn-primary me-2">Previous</button>
                    <span id="pageInfo" class="mt-2"></span>
                    <button id="nextBtn" class="btn btn-primary ms-2">Next</button>
                </div>
            </div>
        </div>

        <div class="offcanvas offcanvas-bottom" tabindex="-1" id="offcanvasBottom" aria-labelledby="offcanvasBottomLabel" style="height: 150px;">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title text-align-center" id="offcanvasBottomLabel"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body d-flex flex-column justify-content-center align-items-center">
                <h4>Are you sure you want to log out?</h4>
            </div>
            <div class="offcanvas-footer d-flex justify-content-center">
                <input type="button" class="btncolorsubmit me-3" id="confirmLogoutBtn" value="Confirm" tabindex="1">
                <input type="button" class="btncolorcancel" data-bs-dismiss="offcanvas" value="Cancel" tabindex="2">
            </div>
        </div>
    </div>
    <script src="~/Scripts/Logoutjs.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {

            const rowsPerPage = 10;
            const rows = document.querySelectorAll("tbody tr");
            const totalRows = rows.length;
            const totalPages = Math.ceil(totalRows / rowsPerPage);

            let currentPage = 1;

            function showPage(page) {
                currentPage = page;

                rows.forEach((row, index) => {
                    row.style.display =
                        (index >= (page - 1) * rowsPerPage &&
                            index < page * rowsPerPage)
                            ? ""
                            : "none";
                });

                document.getElementById("pageInfo").innerText =
                    "Page " + currentPage + " of " + totalPages;
            }

            document.getElementById("prevBtn").addEventListener("click", function () {
                if (currentPage > 1) showPage(currentPage - 1);
            });

            document.getElementById("nextBtn").addEventListener("click", function () {
                if (currentPage < totalPages) showPage(currentPage + 1);
            });

            showPage(1);
        });
    </script>
    <script>
$(document).ready(function () {

    @if (TempData["ErrorMessage"] != null)
    {
        <text>
            Swal.fire({
                icon: 'info',
                title: 'Message',
                text: '@TempData["ErrorMessage"]',
                confirmButtonColor: '#034CAE',
                confirmButtonText: 'OK'
            });
        </text>
    }

});
    </script>

    <script>
$(document).ready(function () {

    /* ================= DROPDOWN OPEN/CLOSE ================= */
    /* ================= DROPDOWN OPEN/CLOSE ================= */

    $(".dropdown-check-list .anchor").click(function (e) {
        e.stopPropagation();
        $(".dropdown-check-list").not($(this).parent()).removeClass("visible"); // close others
        $(this).parent().toggleClass('visible');
    });

    // Prevent closing when clicking inside dropdown
    $(document).on("click", ".dropdown-check-list", function (e) {
        e.stopPropagation();
    });

    // Close only when clicking outside
    $(document).click(function () {
        $('.dropdown-check-list').removeClass('visible');
    });

    /* ================= SELECT ALL PAYMENT ================= */
    $(document).on("change", "#selectAllPayments", function () {
        $(".paymentCheck").prop("checked", this.checked);
    });

    $(document).on("change", ".paymentCheck", function () {
        $("#selectAllPayments").prop(
            "checked",
            $(".paymentCheck").length === $(".paymentCheck:checked").length
        );
    });

    /* ================= SELECT ALL PRODUCT ================= */
    $(document).on("change", "#selectAllItemGroups", function () {
        $(".itemGroupCheck").prop("checked", this.checked);
        loadContracts();
    });

    $(document).on("change", ".itemGroupCheck", function () {

        $("#selectAllItemGroups").prop(
            "checked",
            $(".itemGroupCheck").length === $(".itemGroupCheck:checked").length
        );

        loadContracts();
    });
    /* ================= SELECT ALL PRODUCT ================= */
    $(document).on("change", "#selectAllProducts", function () {
        $(".productCheck").prop("checked", this.checked);
        loadContracts();
    });

    $(document).on("change", ".productCheck", function () {

        $("#selectAllProducts").prop(
            "checked",
            $(".productCheck").length === $(".productCheck:checked").length
        );

        loadContracts();
    });

    /* ================= SELECT ALL CONTRACT ================= */
    $(document).on("change", "#selectAllCategories", function () {
        $(".categoryCheck").prop("checked", this.checked);
    });

    $(document).on("change", ".categoryCheck", function () {
        $("#selectAllCategories").prop(
            "checked",
            $(".categoryCheck").length === $(".categoryCheck:checked").length
        );
    });

    /* ================= LOAD CONTRACT BASED ON PRODUCT ================= */
    function loadContracts() {

        var selectedProducts = [];

        $(".itemGroupCheck:checked").each(function () {
            selectedProducts.push($(this).val());
        });

        $("#itemCategoryList").empty();

        if (selectedProducts.length === 0)
            return;

        $("#itemCategoryList").html("<li>Loading...</li>");

        $.getJSON('@Url.Action("CustomerBasedContract", "ContractExpiryReport")',
            { ProductRecid: selectedProducts.join(",") },
            function (response) {

                $("#itemCategoryList").empty();

                $("#itemCategoryList").append(`
                    <li>
                        <label>
                            <input type="checkbox" id="selectAllCategories">
                            <strong>Select All</strong>
                        </label>
                    </li>
                `);

                $.each(response.data, function (i, contract) {
                    $("#itemCategoryList").append(`
                        <li>
                            <input type="checkbox" class="categoryCheck" value="${contract.Value}" />
                            ${contract.Text}
                        </li>
                    `);
                });

            }).fail(function () {
                $("#itemCategoryList").html("<li>Error loading contracts</li>");
            });
    }

    /* ================= FORM SUBMIT ================= */
    $("#financeForm").submit(function () {

        var selectedProducts = [];
        var selectedContracts = [];
        var selectedCustomers = [];
        var selectedPayments = [];

        // PRODUCTS
        $(".productCheck:checked").each(function () {
            selectedProducts.push($(this).val());
        });

        // CONTRACTS
        $(".itemGroupCheck:checked").each(function () {
            selectedContracts.push($(this).val());
        });

        // CUSTOMERS
        $(".categoryCheck:checked").each(function () {
            selectedCustomers.push($(this).val());
        });

        // PAYMENTS
        $(".paymentCheck:checked").each(function () {
            selectedPayments.push($(this).val());
        });

        $("#ProductRecid").val(selectedProducts.join(","));
        $("#ContractRecid").val(selectedContracts.join(","));
        $("#CustomerRecid").val(selectedCustomers.join(","));
        $("#Type").val(selectedPayments.join(","));

    });


});
    </script>


</body>
</html>


