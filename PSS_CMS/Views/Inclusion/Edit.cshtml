@model PSS_CMS.Models.Inclusion

@{ ViewData["Title"] = "Edit Inclusion";
                ViewBag.PageTitle = "Edit Inclusion";
                Layout = "~/Views/Shared/_Layout.cshtml"; }

@section Styles {
    <link href="~/Content/UIListView.css" rel="stylesheet" />
}

<!-- Breadcrumb -->
<div class="page-breadcrumb">
    <i class="bi bi-shield-check nav-icon"></i>
    <i class="bi bi-chevron-right"></i>
    <a href="@Url.Action("List","ProductMaster")">Warranty Products</a>
    <i class="bi bi-chevron-right"></i>
    @if (Session["P_NAME"] != null)
    {
<a href="@Url.Action("List","ProductMaster")">@Session["P_NAME"].ToString()</a>
                <i class="bi bi-chevron-right"></i>}
    Inclusion
    <i class="bi bi-chevron-right"></i>
    @if (Session["IN_DESCRIPTION"] != null)
    {
<a href="@Url.Action("List","Inclusion")">@Session["IN_DESCRIPTION"].ToString()</a>
                <i class="bi bi-chevron-right"></i>}
    <span>Edit</span>
</div>

<!-- Main Card -->
@using (Html.BeginForm("Edit", "Inclusion", FormMethod.Post,
    new { id = "editform", enctype = "multipart/form-data", @class = "no-preloader" }))
{
<div class="page-card">

    <!-- Header -->
    <div class="page-card-header">
        <h6 class="page-card-title">
            <span class="title-icon">
                <i class="bi bi-plus-circle"></i>
            </span>
            Edit Inclusion
        </h6>
    </div>

    <!-- Form body -->
    <div class="form-body">

        <p class="form-section-title">Inclusion Details</p>

        <div class="form-grid">

            <!-- Description -->
            <div class="form-group">
                <label class="form-label" for="IN_DESCRIPTION">
                    @Html.DisplayNameFor(m => m.IN_DESCRIPTION)
                    <span class="req">*</span>
                </label>
                @Html.TextBoxFor(m => m.IN_DESCRIPTION, new
                {
                    @class = "form-input-text",
                    autocomplete = "off",
                    placeholder = "Enter inclusion description"
                })
                @Html.ValidationMessageFor(m => m.IN_DESCRIPTION, "", new { @class = "text-danger", style = "font-size:11px" })
            </div>

            <!-- Sort Order -->
            <div class="form-group">
                <label class="form-label" for="IN_SORTORDER">
                    @Html.DisplayNameFor(m => m.IN_SORTORDER)
                </label>
                @Html.TextBoxFor(m => m.IN_SORTORDER, new
                {
                    @class = "form-input-num",
                    autocomplete = "off",
                    placeholder = "0",
                    type = "number",
                    min = "0"
                })
            </div>

        </div>

        <div class="form-divider"></div>
        <p class="form-section-title">Settings</p>

        <div class="form-grid">

            <!-- Disable Inclusion checkbox -->
            <label class="check-row" for="IN_InclusionDisable">
                @Html.CheckBoxFor(m => m.IN_InclusionDisable,
                         new { @class = "form-check-input", id = "IN_InclusionDisable" })
                <div class="check-row-label">
                    <span>@Html.DisplayNameFor(m => m.IN_InclusionDisable)</span>
                    <small>Hide this inclusion from active listings</small>
                </div>
            </label>

        </div>

    </div>

    <!-- Legend bar — matches list view style -->
    <div class="legend-bar">

        <div class="legend-item">
            <span style="display:inline-block;width:12px;height:12px;border-radius:3px;background:var(--primary);flex-shrink:0;"></span>
            <span>Text Field</span>
        </div>

        <div class="legend-item">
            <span style="display:inline-block;width:12px;height:12px;border-radius:3px;background:#185FA5;flex-shrink:0;"></span>
            <span>Number Field</span>
        </div>

        

    </div>

    <!-- Actions -->
    <div class="form-actions">
        <a href="@Url.Action("List","Inclusion")"
           class="btn-cancel">
            <i class="bi bi-x-lg"></i> Cancel
        </a>
        <button type="submit"
                class="btn-save"
                id="savebutton">
            <i class="bi bi-floppy"></i> Update
        </button>
    </div>

</div>}

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        $(document).ready(function () {

            /* ── AJAX submit — processing spinner shows FIRST ── */
            $("#editform").submit(function (e) {
                e.preventDefault();

                var btn      = $("#savebutton");
                var formData = new FormData(this);

                btn.prop("disabled", true);

                // ① Show Processing spinner first, fire AJAX inside didOpen
                Swal.fire({
                    title: "Processing…",
                    html: "Please wait while we save your changes.",
                    allowOutsideClick: false,
                    allowEscapeKey: false,
                    showConfirmButton: false,
                    didOpen: function () {
                        Swal.showLoading();

                        $.ajax({
                            url: '@Url.Action("Edit","Inclusion")',
                            type: "POST",
                            data: formData,
                            processData: false,
                            contentType: false,
                            success: function (response) {
                                if (response.success) {
                                    // ② Small delay so spinner is visible, then show success
                                    setTimeout(function () {
                                        Swal.fire({
                                            title: "Success!",
                                            text: response.message,
                                            icon: "success",
                                            confirmButtonText: "Ok",
                                            allowOutsideClick: false,
                                            allowEscapeKey: false,
                                            customClass: {
                                                confirmButton: "swal-btn-continue"
                                            }
                                        }).then(function () {
                                            window.location.href = '@Url.Action("List","Inclusion")';
                                        });
                                    }, 800);
                                } else {
                                    Swal.fire({
                                        title: "Message",
                                        text: response.message,
                                        icon: "info",
                                        allowOutsideClick: false,
                                        allowEscapeKey: false
                                    });
                                    resetButton();
                                }
                            },
                            error: function () {
                                Swal.fire({
                                    title: "Error!",
                                    text: "An unexpected error occurred.",
                                    icon: "error",
                                    allowOutsideClick: false,
                                    allowEscapeKey: false
                                });
                                resetButton();
                            }
                        });
                    }
                });
            });

            function resetButton() {
                $("#savebutton").prop("disabled", false);
            }
        });
    </script>
}
